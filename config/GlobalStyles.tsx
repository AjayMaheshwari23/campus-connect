import { StyleSheet } from "react-native";

export default StyleSheet.create({
  HView: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },
  VView: {
    display: "flex",
    flexDirection: "column",
  },
});
